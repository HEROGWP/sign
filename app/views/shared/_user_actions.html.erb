<% unless current_user?(user) %>
  <% if current_user.following?(user) %>
    <%= link_to "不關注", unfollow_user_path(user), class: "btn btn-large", method: :delete %>
  <% else %>
	  <%= button_to "關注", follow_user_path(user), class: "btn btn-large btn-primary" %>
  <% end %>
<% end %>