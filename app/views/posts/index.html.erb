<div class="row">
  <aside class="span4">
    <section>
    	<%= render "shared/user_info", user: current_user %>
    </section>
    <section>
      <%= form_for @post do |f| %>
	      <%= render "shared/form_errors", record: @post %>
        <%= f.text_area :content, placeholder: "在想些什麼？" %><br />
        <%= f.submit "留言", class: "btn btn-primary" %>
      <% end %>
    </section>
  </aside>
  <div class="span8">
    <h1>貼文</h1>
    <ul class="posts">
      <%= render partial: "shared/post", collection: @posts %>
    </ul>
  </div>
</div>